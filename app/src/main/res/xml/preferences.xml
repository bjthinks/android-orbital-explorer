<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- <CheckBoxPreference
        android:key="pref_sync"
        android:title="@string/pref_sync"
        android:summary="@string/pref_sync_summ"
        android:defaultValue="true" /> -->
    <ListPreference
        android:key="prefColor"
        android:title="@string/prefColorTitle"
        android:dialogTitle="@string/prefColorTitle"
        android:entries="@array/prefColorEntries"
        android:entryValues="@array/prefColorValues"
        android:defaultValue="@string/prefColorDefault" />
</PreferenceScreen>

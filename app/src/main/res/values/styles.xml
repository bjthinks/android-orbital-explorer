<resources>

    <style name="OpaqueTheme" parent="android:Theme.Holo" />

    <style name="TransparentTheme" parent="android:Theme.Holo">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:actionBarStyle">@style/TransparentActionBar</item>
    </style>

    <!-- TODO make icon and test if it shows on Holo action bar -->
    <style name="TransparentActionBar" parent="android:Widget.Holo.ActionBar">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
